#include "main.h"

/**
 * _abs - Write a function that computes
 * the absolute value of an integer
 * @i: the int to check
 * Return: the absolute value of int
 */

int _abs(int)
{
if (i >= 0)
{
return (i);
}
return (-i);
}
