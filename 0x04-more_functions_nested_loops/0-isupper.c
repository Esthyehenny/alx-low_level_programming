#include "main.h"

/**
 * _isupper - Write a function that checks for
 * uppercase character
 * @c: The character to be checked
 * Return: 1 if c is in uppercase, 0 if not
 */

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
